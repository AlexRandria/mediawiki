- name: Install epel release
  package:
    name: epel-release
    state: present
    update_cache: yes
- name: copier rpm file sur le serveur
  copy:
     src: files/remi-release-7.rpm
     dest: /tmp_app/mediawiki/package.rpm

- name: Install package.
  yum:
     name: /tmp_app/mediawiki/package.rpm
     state: present
